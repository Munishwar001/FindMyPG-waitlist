# Paste your Supabase or Neon Postgres connection string here.
# Supabase: Project Settings -> Database -> Connection string (URI, use the "pooler" one for serverless)
# Neon: Dashboard -> Connection Details -> Connection string
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"
